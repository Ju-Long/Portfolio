---
import type { Feature } from "@model/app";

interface Props {
    features: Feature[]
}

const { features } = Astro.props
---
<div class="swiper max-w-dvw z-10">
    <div class="swiper-wrapper max-w-dvw z-10">
        {
            features.map((feature) => (
                <div class="swiper-slide w-dvw md:max-w-[50dvw] z-10">
                    <img 
                        src={feature.image} 
                        alt="" 
                        class="w-dvw md:w-[50dvw] aspect-auto rounded-md"
                        />
                </div>
            ))
        }
    </div>
</div>